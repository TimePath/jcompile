rootProject.name = 'jcompile'

include 'api'

include 'frontends:kotlin', 'frontends:kotlin:grammar'
include 'frontends:quakec', 'frontends:quakec:grammar'

include 'backends:cpp'
include 'backends:q1vm'

